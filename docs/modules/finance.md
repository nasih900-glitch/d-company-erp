# Module: FINANCE

See `docs/MODULES.md` for the one-paragraph orientation. Deep build lands in a follow-up session.

## Endpoints

Defined in `backend/app/api/v1/finance/router.py`. Inspect at `http://localhost:8000/docs` once the backend is running.

## TODO (deep build)

- [ ] Services + repository implementations beyond the router stub
- [ ] Domain events emitted on key transitions
- [ ] Tests: unit (service-level) and integration (endpoint + DB)
- [ ] Frontend integration with React Query
- [ ] RBAC checks reviewed against `backend/app/core/permissions.py`
