<?xml version="1.0" encoding="utf-8"?>
<!--
    The Room database contains sales/outbox recovery data and SharedPreferences
    contain an encrypted session envelope. Neither may be copied into cloud
    backup on pre-Android-12 devices. Recovery is performed from the ERP
    server after the tablet is deliberately enrolled again.
-->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="file" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="external" path="." />
    <exclude domain="device_root" path="." />
    <exclude domain="device_file" path="." />
    <exclude domain="device_database" path="." />
    <exclude domain="device_sharedpref" path="." />
</full-backup-content>
